logger:
  level: DEBUG

json:

http_request:
  timeout: 15s
  follow_redirects: true
  verify_ssl: false
  useragent: GuitionP4Panel/1.0

i2c:
  sda: GPIO7
  scl: GPIO8
  scan: true
  frequency: 400kHz
  id: bus_a

api:
  encryption:
    key: !secret api_encryption_key
  # Default is 5; we've seen the dashboard's stuck "esphome logs" subprocesses
  # plus HA + an occasional debug session exhaust slots and lock out HA.
  max_connections: 12
  # Only run setup/teardown on first-connect / last-disconnect.
  # Multiple clients connect (HA, log streams, etc.) — running refresh and
  # voice_assistant.start_continuous on every connect blocked the API loop
  # and cascaded into socket exhaustion.
  on_client_connected:
    - globals.set:
        id: g_ha_client_count
        value: !lambda 'return id(g_ha_client_count) + 1;'
    - if:
        condition:
          lambda: 'return id(g_ha_client_count) == 1;'
        then:
          - globals.set:
              id: g_ha_connected
              value: 'true'
          - lvgl.widget.hide: ha_disconnected_overlay
          - lvgl.widget.show: tap_zone_btn
          - lvgl.label.update:
              id: dash_error_lbl
              text: ""
          - script.execute: refresh_immich_slideshow
          - voice_assistant.start_continuous:
  on_client_disconnected:
    - globals.set:
        id: g_ha_client_count
        value: !lambda |-
          int c = id(g_ha_client_count) - 1;
          return c < 0 ? 0 : c;
    - if:
        condition:
          lambda: 'return id(g_ha_client_count) == 0;'
        then:
          - globals.set:
              id: g_ha_connected
              value: 'false'
          - lvgl.widget.show: ha_disconnected_overlay
          - lvgl.widget.hide: tap_zone_btn
          - lvgl.page.show: slideshow_page
          - lvgl.label.update:
              id: dash_error_lbl
              text: "Home Assistant disconnected"
          - voice_assistant.stop:

ota:
  - platform: esphome
    password: !secret ota_password

# After repeated boot crashes the device enters safe mode (OTA-only, no user
# code) for 1 minute — gives a reliable window to OTA-flash a fixed firmware
# even if normal boot crashes immediately.
safe_mode:
  boot_is_good_after: 60s
  reboot_timeout: 1min
  num_attempts: 5

time:
  - platform: homeassistant
    id: ha_time

text_sensor:
  - platform: wifi_info
    ip_address:
      name: "Panel IP Address"
      id: wifi_ip_addr
    ssid:
      name: "Panel Wi-Fi SSID"

  # Displays proxy URL in settings; no auto-refresh on change to avoid
  # cascading download attempts every time HA pushes a state update.
  - platform: homeassistant
    id: ha_immich_url_sensor
    entity_id: input_text.panel_immich_url
    internal: true
    on_value:
      then:
        - lambda: |-
            if (!x.empty()) id(immich_base_url) = x.c_str();

  # Slideshow refresh interval, in minutes. Reads from HA helper
  # input_number.panel_slideshow_interval (recommended range 1–1440 minutes;
  # create with min=1, max=1440, step=1 mode=box). Clamped here so a bad
  # value in HA can't break the timer logic. The 1-minute interval block
  # in display_touch_board.yaml reads g_slideshow_interval_min on every tick.
  - platform: homeassistant
    id: ha_slideshow_interval
    entity_id: input_number.panel_slideshow_interval
    internal: true
    on_value:
      then:
        - lambda: |-
            int v = (int)atof(x.c_str());
            if (v < 1) v = 1;
            if (v > 1440) v = 1440;
            id(g_slideshow_interval_min) = v;
            id(g_slideshow_counter_min) = 0;

  # Calendar events pushed from HA automation into input_text.panel_calendar_events
  - platform: homeassistant
    id: ha_calendar_events
    entity_id: input_text.panel_calendar_events
    internal: true
    on_value:
      then:
        - lambda: |-
            lv_obj_t* labels[8] = {
              id(cal_evt_0), id(cal_evt_1), id(cal_evt_2), id(cal_evt_3),
              id(cal_evt_4), id(cal_evt_5), id(cal_evt_6), id(cal_evt_7)
            };
            lv_obj_t* row_objs[8] = {
              id(cal_row_0), id(cal_row_1), id(cal_row_2), id(cal_row_3),
              id(cal_row_4), id(cal_row_5), id(cal_row_6), id(cal_row_7)
            };
            std::string text = x.empty() ? "No events this week" : x;
            // Show first row with "No events" if empty, else fill rows per event
            bool no_events = x.empty();
            for (int i = 0; i < 8; i++) {
              std::string line = "";
              if (!text.empty()) {
                auto nl = text.find('\n');
                line = (nl != std::string::npos) ? text.substr(0, nl) : text;
                text = (nl != std::string::npos) ? text.substr(nl + 1) : "";
              }
              lv_label_set_text(labels[i], line.c_str());
              if (!line.empty()) {
                lv_obj_clear_flag(row_objs[i], LV_OBJ_FLAG_HIDDEN);
              } else {
                lv_obj_add_flag(row_objs[i], LV_OBJ_FLAG_HIDDEN);
              }
            }

  # ─── Room dashboard: light state + brightness + temp/humidity ─────────
  # Each room card subscribes to light state ("on"/"off"), updates the bulb
  # tint and ON/OFF label. Dimmable rooms (2-5) also subscribe to brightness
  # and drive the arc. Temp/humidity update text labels.
  - platform: homeassistant
    id: room1_state
    entity_id: "${room1_light}"
    internal: true
    on_value:
      then:
        - lambda: |-
            bool on = (x == "on");
            lv_label_set_text(id(room1_state_lbl), on ? "ON" : "OFF");
            lv_obj_set_style_text_color(id(room1_state_lbl), on ? lv_color_hex(0xFFD700) : lv_color_hex(0x666666), 0);
            lv_obj_set_style_img_recolor(id(room1_icon), on ? lv_color_hex(0xFFD700) : lv_color_hex(0x555555), 0);
            lv_obj_set_style_img_recolor_opa(id(room1_icon), LV_OPA_COVER, 0);
            // Maintain the all-lights bitmask + refresh the master button label.
            if (on) id(g_lights_on_mask) |= (1 << 0);
            else    id(g_lights_on_mask) &= ~(1 << 0);
            bool any = id(g_lights_on_mask) != 0;
            lv_label_set_text(id(all_lights_lbl), any ? "All Lights Off" : "All Lights On");
            lv_obj_set_style_text_color(id(all_lights_lbl), any ? lv_color_hex(0xFFAACC) : lv_color_hex(0xFFD700), 0);
  - platform: homeassistant
    id: room1_temp_s
    entity_id: "${room1_temp}"
    internal: true
    on_value:
      then:
        - lambda: |-
            lv_label_set_text(id(room1_temp_lbl), (x + "\xC2\xB0").c_str());
  - platform: homeassistant
    id: room1_hum_s
    entity_id: "${room1_humidity}"
    internal: true
    on_value:
      then:
        - lambda: |-
            lv_label_set_text(id(room1_hum_lbl), (x + "%").c_str());

  - platform: homeassistant
    id: room2_state
    entity_id: "${room2_light}"
    internal: true
    on_value:
      then:
        - lambda: |-
            bool on = (x == "on");
            lv_label_set_text(id(room2_state_lbl), on ? "ON" : "OFF");
            lv_obj_set_style_text_color(id(room2_state_lbl), on ? lv_color_hex(0xFFD700) : lv_color_hex(0x666666), 0);
            lv_obj_set_style_img_recolor(id(room2_icon), on ? lv_color_hex(0xFFD700) : lv_color_hex(0x555555), 0);
            lv_obj_set_style_img_recolor_opa(id(room2_icon), LV_OPA_COVER, 0);
            // Maintain the all-lights bitmask + refresh the master button label.
            if (on) id(g_lights_on_mask) |= (1 << 1);
            else    id(g_lights_on_mask) &= ~(1 << 1);
            bool any = id(g_lights_on_mask) != 0;
            lv_label_set_text(id(all_lights_lbl), any ? "All Lights Off" : "All Lights On");
            lv_obj_set_style_text_color(id(all_lights_lbl), any ? lv_color_hex(0xFFAACC) : lv_color_hex(0xFFD700), 0);
  - platform: homeassistant
    id: room2_brightness
    entity_id: "${room2_light}"
    attribute: brightness
    internal: true
    on_value:
      then:
        - lambda: |-
            int b = atoi(x.c_str());
            int pct = (b * 100) / 255;
            if (pct < 0) pct = 0;
            if (pct > 100) pct = 100;
            lv_slider_set_value(id(room2_arc), pct, LV_ANIM_OFF);
  - platform: homeassistant
    id: room2_temp_s
    entity_id: "${room2_temp}"
    internal: true
    on_value:
      then:
        - lambda: |-
            lv_label_set_text(id(room2_temp_lbl), (x + "\xC2\xB0").c_str());
  - platform: homeassistant
    id: room2_hum_s
    entity_id: "${room2_humidity}"
    internal: true
    on_value:
      then:
        - lambda: |-
            lv_label_set_text(id(room2_hum_lbl), (x + "%").c_str());

  - platform: homeassistant
    id: room3_state
    entity_id: "${room3_light}"
    internal: true
    on_value:
      then:
        - lambda: |-
            bool on = (x == "on");
            lv_label_set_text(id(room3_state_lbl), on ? "ON" : "OFF");
            lv_obj_set_style_text_color(id(room3_state_lbl), on ? lv_color_hex(0xFFD700) : lv_color_hex(0x666666), 0);
            lv_obj_set_style_img_recolor(id(room3_icon), on ? lv_color_hex(0xFFD700) : lv_color_hex(0x555555), 0);
            lv_obj_set_style_img_recolor_opa(id(room3_icon), LV_OPA_COVER, 0);
            // Maintain the all-lights bitmask + refresh the master button label.
            if (on) id(g_lights_on_mask) |= (1 << 2);
            else    id(g_lights_on_mask) &= ~(1 << 2);
            bool any = id(g_lights_on_mask) != 0;
            lv_label_set_text(id(all_lights_lbl), any ? "All Lights Off" : "All Lights On");
            lv_obj_set_style_text_color(id(all_lights_lbl), any ? lv_color_hex(0xFFAACC) : lv_color_hex(0xFFD700), 0);
  - platform: homeassistant
    id: room3_brightness
    entity_id: "${room3_light}"
    attribute: brightness
    internal: true
    on_value:
      then:
        - lambda: |-
            int b = atoi(x.c_str());
            int pct = (b * 100) / 255;
            if (pct < 0) pct = 0;
            if (pct > 100) pct = 100;
            lv_slider_set_value(id(room3_arc), pct, LV_ANIM_OFF);

  - platform: homeassistant
    id: room4_state
    entity_id: "${room4_light}"
    internal: true
    on_value:
      then:
        - lambda: |-
            bool on = (x == "on");
            lv_label_set_text(id(room4_state_lbl), on ? "ON" : "OFF");
            lv_obj_set_style_text_color(id(room4_state_lbl), on ? lv_color_hex(0xFFD700) : lv_color_hex(0x666666), 0);
            lv_obj_set_style_img_recolor(id(room4_icon), on ? lv_color_hex(0xFFD700) : lv_color_hex(0x555555), 0);
            lv_obj_set_style_img_recolor_opa(id(room4_icon), LV_OPA_COVER, 0);
            // Maintain the all-lights bitmask + refresh the master button label.
            if (on) id(g_lights_on_mask) |= (1 << 3);
            else    id(g_lights_on_mask) &= ~(1 << 3);
            bool any = id(g_lights_on_mask) != 0;
            lv_label_set_text(id(all_lights_lbl), any ? "All Lights Off" : "All Lights On");
            lv_obj_set_style_text_color(id(all_lights_lbl), any ? lv_color_hex(0xFFAACC) : lv_color_hex(0xFFD700), 0);
  - platform: homeassistant
    id: room4_brightness
    entity_id: "${room4_light}"
    attribute: brightness
    internal: true
    on_value:
      then:
        - lambda: |-
            int b = atoi(x.c_str());
            int pct = (b * 100) / 255;
            if (pct < 0) pct = 0;
            if (pct > 100) pct = 100;
            lv_slider_set_value(id(room4_arc), pct, LV_ANIM_OFF);
  - platform: homeassistant
    id: room4_temp_s
    entity_id: "${room4_temp}"
    internal: true
    on_value:
      then:
        - lambda: |-
            lv_label_set_text(id(room4_temp_lbl), (x + "\xC2\xB0").c_str());
  - platform: homeassistant
    id: room4_hum_s
    entity_id: "${room4_humidity}"
    internal: true
    on_value:
      then:
        - lambda: |-
            lv_label_set_text(id(room4_hum_lbl), (x + "%").c_str());

  - platform: homeassistant
    id: room5_state
    entity_id: "${room5_light}"
    internal: true
    on_value:
      then:
        - lambda: |-
            bool on = (x == "on");
            lv_label_set_text(id(room5_state_lbl), on ? "ON" : "OFF");
            lv_obj_set_style_text_color(id(room5_state_lbl), on ? lv_color_hex(0xFFD700) : lv_color_hex(0x666666), 0);
            lv_obj_set_style_img_recolor(id(room5_icon), on ? lv_color_hex(0xFFD700) : lv_color_hex(0x555555), 0);
            lv_obj_set_style_img_recolor_opa(id(room5_icon), LV_OPA_COVER, 0);
            // Maintain the all-lights bitmask + refresh the master button label.
            if (on) id(g_lights_on_mask) |= (1 << 4);
            else    id(g_lights_on_mask) &= ~(1 << 4);
            bool any = id(g_lights_on_mask) != 0;
            lv_label_set_text(id(all_lights_lbl), any ? "All Lights Off" : "All Lights On");
            lv_obj_set_style_text_color(id(all_lights_lbl), any ? lv_color_hex(0xFFAACC) : lv_color_hex(0xFFD700), 0);
  - platform: homeassistant
    id: room5_brightness
    entity_id: "${room5_light}"
    attribute: brightness
    internal: true
    on_value:
      then:
        - lambda: |-
            int b = atoi(x.c_str());
            int pct = (b * 100) / 255;
            if (pct < 0) pct = 0;
            if (pct > 100) pct = 100;
            lv_slider_set_value(id(room5_arc), pct, LV_ANIM_OFF);
  - platform: homeassistant
    id: room5_temp_s
    entity_id: "${room5_temp}"
    internal: true
    on_value:
      then:
        - lambda: |-
            lv_label_set_text(id(room5_temp_lbl), (x + "\xC2\xB0").c_str());
  - platform: homeassistant
    id: room5_hum_s
    entity_id: "${room5_humidity}"
    internal: true
    on_value:
      then:
        - lambda: |-
            lv_label_set_text(id(room5_hum_lbl), (x + "%").c_str());

  - platform: homeassistant
    id: room6_state
    entity_id: "${room6_light}"
    internal: true
    on_value:
      then:
        - lambda: |-
            bool on = (x == "on");
            lv_label_set_text(id(room6_state_lbl), on ? "ON" : "OFF");
            lv_obj_set_style_text_color(id(room6_state_lbl), on ? lv_color_hex(0xFFD700) : lv_color_hex(0x666666), 0);
            lv_obj_set_style_img_recolor(id(room6_icon), on ? lv_color_hex(0xFFD700) : lv_color_hex(0x555555), 0);
            lv_obj_set_style_img_recolor_opa(id(room6_icon), LV_OPA_COVER, 0);
            // Maintain the all-lights bitmask + refresh the master button label.
            if (on) id(g_lights_on_mask) |= (1 << 5);
            else    id(g_lights_on_mask) &= ~(1 << 5);
            bool any = id(g_lights_on_mask) != 0;
            lv_label_set_text(id(all_lights_lbl), any ? "All Lights Off" : "All Lights On");
            lv_obj_set_style_text_color(id(all_lights_lbl), any ? lv_color_hex(0xFFAACC) : lv_color_hex(0xFFD700), 0);

  # ─── Tado page: per-zone state, current temp/humidity, target temp ───
  # State (off/auto/heat) drives the mode buttons' highlight. Target temp
  # updates a global the +/− buttons mutate; current temp/humidity drive
  # the big readout. Each card has 4 HA subscriptions = 24 total.
  - platform: homeassistant
    id: tado1_state
    entity_id: "${tado1_entity}"
    internal: true
    on_value:
      then:
        - lambda: |-
            std::string m = x;
            const char* lbl = (m == "off") ? "OFF" : (m == "heat") ? "HEAT" : "AUTO";
            lv_label_set_text(id(tado1_mode_lbl), lbl);
            uint32_t c = (m == "off") ? 0x666666 : (m == "heat") ? 0xFF8844 : 0x66AAFF;
            lv_obj_set_style_text_color(id(tado1_mode_lbl), lv_color_hex(c), 0);
  - platform: homeassistant
    id: tado1_ct
    entity_id: "${tado1_entity}"
    attribute: current_temperature
    internal: true
    on_value:
      then:
        - lambda: |-
            char b[10]; snprintf(b, sizeof(b), "%s\xC2\xB0", x.c_str());
            lv_label_set_text(id(tado1_ct_lbl), b);
  - platform: homeassistant
    id: tado1_ch
    entity_id: "${tado1_entity}"
    attribute: current_humidity
    internal: true
    on_value:
      then:
        - lambda: |-
            char b[12]; snprintf(b, sizeof(b), "%s%%", x.c_str());
            lv_label_set_text(id(tado1_ch_lbl), b);
  - platform: homeassistant
    id: tado1_tgt
    entity_id: "${tado1_entity}"
    attribute: temperature
    internal: true
    on_value:
      then:
        - lambda: |-
            if (x.empty() || x == "None") return;
            float t = atof(x.c_str());
            id(g_tado1_target) = t;
            char b[10]; snprintf(b, sizeof(b), "%.1f\xC2\xB0", t);
            lv_label_set_text(id(tado1_tgt_lbl), b);

  - platform: homeassistant
    id: tado2_state
    entity_id: "${tado2_entity}"
    internal: true
    on_value:
      then:
        - lambda: |-
            std::string m = x;
            const char* lbl = (m == "off") ? "OFF" : (m == "heat") ? "HEAT" : "AUTO";
            lv_label_set_text(id(tado2_mode_lbl), lbl);
            uint32_t c = (m == "off") ? 0x666666 : (m == "heat") ? 0xFF8844 : 0x66AAFF;
            lv_obj_set_style_text_color(id(tado2_mode_lbl), lv_color_hex(c), 0);
  - platform: homeassistant
    id: tado2_ct
    entity_id: "${tado2_entity}"
    attribute: current_temperature
    internal: true
    on_value:
      then:
        - lambda: |-
            char b[10]; snprintf(b, sizeof(b), "%s\xC2\xB0", x.c_str());
            lv_label_set_text(id(tado2_ct_lbl), b);
  - platform: homeassistant
    id: tado2_ch
    entity_id: "${tado2_entity}"
    attribute: current_humidity
    internal: true
    on_value:
      then:
        - lambda: |-
            char b[12]; snprintf(b, sizeof(b), "%s%%", x.c_str());
            lv_label_set_text(id(tado2_ch_lbl), b);
  - platform: homeassistant
    id: tado2_tgt
    entity_id: "${tado2_entity}"
    attribute: temperature
    internal: true
    on_value:
      then:
        - lambda: |-
            if (x.empty() || x == "None") return;
            float t = atof(x.c_str());
            id(g_tado2_target) = t;
            char b[10]; snprintf(b, sizeof(b), "%.1f\xC2\xB0", t);
            lv_label_set_text(id(tado2_tgt_lbl), b);

  - platform: homeassistant
    id: tado3_state
    entity_id: "${tado3_entity}"
    internal: true
    on_value:
      then:
        - lambda: |-
            std::string m = x;
            const char* lbl = (m == "off") ? "OFF" : (m == "heat") ? "HEAT" : "AUTO";
            lv_label_set_text(id(tado3_mode_lbl), lbl);
            uint32_t c = (m == "off") ? 0x666666 : (m == "heat") ? 0xFF8844 : 0x66AAFF;
            lv_obj_set_style_text_color(id(tado3_mode_lbl), lv_color_hex(c), 0);
  - platform: homeassistant
    id: tado3_ct
    entity_id: "${tado3_entity}"
    attribute: current_temperature
    internal: true
    on_value:
      then:
        - lambda: |-
            char b[10]; snprintf(b, sizeof(b), "%s\xC2\xB0", x.c_str());
            lv_label_set_text(id(tado3_ct_lbl), b);
  - platform: homeassistant
    id: tado3_ch
    entity_id: "${tado3_entity}"
    attribute: current_humidity
    internal: true
    on_value:
      then:
        - lambda: |-
            char b[12]; snprintf(b, sizeof(b), "%s%%", x.c_str());
            lv_label_set_text(id(tado3_ch_lbl), b);
  - platform: homeassistant
    id: tado3_tgt
    entity_id: "${tado3_entity}"
    attribute: temperature
    internal: true
    on_value:
      then:
        - lambda: |-
            if (x.empty() || x == "None") return;
            float t = atof(x.c_str());
            id(g_tado3_target) = t;
            char b[10]; snprintf(b, sizeof(b), "%.1f\xC2\xB0", t);
            lv_label_set_text(id(tado3_tgt_lbl), b);

  - platform: homeassistant
    id: tado4_state
    entity_id: "${tado4_entity}"
    internal: true
    on_value:
      then:
        - lambda: |-
            std::string m = x;
            const char* lbl = (m == "off") ? "OFF" : (m == "heat") ? "HEAT" : "AUTO";
            lv_label_set_text(id(tado4_mode_lbl), lbl);
            uint32_t c = (m == "off") ? 0x666666 : (m == "heat") ? 0xFF8844 : 0x66AAFF;
            lv_obj_set_style_text_color(id(tado4_mode_lbl), lv_color_hex(c), 0);
  - platform: homeassistant
    id: tado4_ct
    entity_id: "${tado4_entity}"
    attribute: current_temperature
    internal: true
    on_value:
      then:
        - lambda: |-
            char b[10]; snprintf(b, sizeof(b), "%s\xC2\xB0", x.c_str());
            lv_label_set_text(id(tado4_ct_lbl), b);
  - platform: homeassistant
    id: tado4_ch
    entity_id: "${tado4_entity}"
    attribute: current_humidity
    internal: true
    on_value:
      then:
        - lambda: |-
            char b[12]; snprintf(b, sizeof(b), "%s%%", x.c_str());
            lv_label_set_text(id(tado4_ch_lbl), b);
  - platform: homeassistant
    id: tado4_tgt
    entity_id: "${tado4_entity}"
    attribute: temperature
    internal: true
    on_value:
      then:
        - lambda: |-
            if (x.empty() || x == "None") return;
            float t = atof(x.c_str());
            id(g_tado4_target) = t;
            char b[10]; snprintf(b, sizeof(b), "%.1f\xC2\xB0", t);
            lv_label_set_text(id(tado4_tgt_lbl), b);

  - platform: homeassistant
    id: tado5_state
    entity_id: "${tado5_entity}"
    internal: true
    on_value:
      then:
        - lambda: |-
            std::string m = x;
            const char* lbl = (m == "off") ? "OFF" : (m == "heat") ? "HEAT" : "AUTO";
            lv_label_set_text(id(tado5_mode_lbl), lbl);
            uint32_t c = (m == "off") ? 0x666666 : (m == "heat") ? 0xFF8844 : 0x66AAFF;
            lv_obj_set_style_text_color(id(tado5_mode_lbl), lv_color_hex(c), 0);
  - platform: homeassistant
    id: tado5_ct
    entity_id: "${tado5_entity}"
    attribute: current_temperature
    internal: true
    on_value:
      then:
        - lambda: |-
            char b[10]; snprintf(b, sizeof(b), "%s\xC2\xB0", x.c_str());
            lv_label_set_text(id(tado5_ct_lbl), b);
  - platform: homeassistant
    id: tado5_ch
    entity_id: "${tado5_entity}"
    attribute: current_humidity
    internal: true
    on_value:
      then:
        - lambda: |-
            char b[12]; snprintf(b, sizeof(b), "%s%%", x.c_str());
            lv_label_set_text(id(tado5_ch_lbl), b);
  - platform: homeassistant
    id: tado5_tgt
    entity_id: "${tado5_entity}"
    attribute: temperature
    internal: true
    on_value:
      then:
        - lambda: |-
            if (x.empty() || x == "None") return;
            float t = atof(x.c_str());
            id(g_tado5_target) = t;
            char b[10]; snprintf(b, sizeof(b), "%.1f\xC2\xB0", t);
            lv_label_set_text(id(tado5_tgt_lbl), b);

  - platform: homeassistant
    id: tado6_state
    entity_id: "${tado6_entity}"
    internal: true
    on_value:
      then:
        - lambda: |-
            std::string m = x;
            const char* lbl = (m == "off") ? "OFF" : (m == "heat") ? "HEAT" : "AUTO";
            lv_label_set_text(id(tado6_mode_lbl), lbl);
            uint32_t c = (m == "off") ? 0x666666 : (m == "heat") ? 0xFF8844 : 0x66AAFF;
            lv_obj_set_style_text_color(id(tado6_mode_lbl), lv_color_hex(c), 0);
  - platform: homeassistant
    id: tado6_ct
    entity_id: "${tado6_entity}"
    attribute: current_temperature
    internal: true
    on_value:
      then:
        - lambda: |-
            char b[10]; snprintf(b, sizeof(b), "%s\xC2\xB0", x.c_str());
            lv_label_set_text(id(tado6_ct_lbl), b);
  - platform: homeassistant
    id: tado6_ch
    entity_id: "${tado6_entity}"
    attribute: current_humidity
    internal: true
    on_value:
      then:
        - lambda: |-
            char b[12]; snprintf(b, sizeof(b), "%s%%", x.c_str());
            lv_label_set_text(id(tado6_ch_lbl), b);
  - platform: homeassistant
    id: tado6_tgt
    entity_id: "${tado6_entity}"
    attribute: temperature
    internal: true
    on_value:
      then:
        - lambda: |-
            if (x.empty() || x == "None") return;
            float t = atof(x.c_str());
            id(g_tado6_target) = t;
            char b[10]; snprintf(b, sizeof(b), "%.1f\xC2\xB0", t);
            lv_label_set_text(id(tado6_tgt_lbl), b);

  # ─── Presence page: 6 binary sensors ──────────────────────────────────
  # State "on" → green dot + ACTIVE; "off"/anything else → grey + CLEAR.
  - platform: homeassistant
    id: pres1_state
    entity_id: "${pres1_entity}"
    internal: true
    on_value:
      then:
        - lambda: |-
            bool on = (x == "on");
            lv_label_set_text(id(pres1_lbl), on ? "ACTIVE" : "CLEAR");
            lv_obj_set_style_text_color(id(pres1_lbl), on ? lv_color_hex(0x10D080) : lv_color_hex(0x666666), 0);
            lv_obj_set_style_img_recolor(id(pres1_dot), on ? lv_color_hex(0x10D080) : lv_color_hex(0x444444), 0);
            lv_obj_set_style_img_recolor_opa(id(pres1_dot), LV_OPA_COVER, 0);
  - platform: homeassistant
    id: pres2_state
    entity_id: "${pres2_entity}"
    internal: true
    on_value:
      then:
        - lambda: |-
            bool on = (x == "on");
            lv_label_set_text(id(pres2_lbl), on ? "ACTIVE" : "CLEAR");
            lv_obj_set_style_text_color(id(pres2_lbl), on ? lv_color_hex(0x10D080) : lv_color_hex(0x666666), 0);
            lv_obj_set_style_img_recolor(id(pres2_dot), on ? lv_color_hex(0x10D080) : lv_color_hex(0x444444), 0);
            lv_obj_set_style_img_recolor_opa(id(pres2_dot), LV_OPA_COVER, 0);
  - platform: homeassistant
    id: pres3_state
    entity_id: "${pres3_entity}"
    internal: true
    on_value:
      then:
        - lambda: |-
            bool on = (x == "on");
            lv_label_set_text(id(pres3_lbl), on ? "ACTIVE" : "CLEAR");
            lv_obj_set_style_text_color(id(pres3_lbl), on ? lv_color_hex(0x10D080) : lv_color_hex(0x666666), 0);
            lv_obj_set_style_img_recolor(id(pres3_dot), on ? lv_color_hex(0x10D080) : lv_color_hex(0x444444), 0);
            lv_obj_set_style_img_recolor_opa(id(pres3_dot), LV_OPA_COVER, 0);
  - platform: homeassistant
    id: pres4_state
    entity_id: "${pres4_entity}"
    internal: true
    on_value:
      then:
        - lambda: |-
            bool on = (x == "on");
            lv_label_set_text(id(pres4_lbl), on ? "ACTIVE" : "CLEAR");
            lv_obj_set_style_text_color(id(pres4_lbl), on ? lv_color_hex(0x10D080) : lv_color_hex(0x666666), 0);
            lv_obj_set_style_img_recolor(id(pres4_dot), on ? lv_color_hex(0x10D080) : lv_color_hex(0x444444), 0);
            lv_obj_set_style_img_recolor_opa(id(pres4_dot), LV_OPA_COVER, 0);
  - platform: homeassistant
    id: pres5_state
    entity_id: "${pres5_entity}"
    internal: true
    on_value:
      then:
        - lambda: |-
            bool on = (x == "on");
            lv_label_set_text(id(pres5_lbl), on ? "ACTIVE" : "CLEAR");
            lv_obj_set_style_text_color(id(pres5_lbl), on ? lv_color_hex(0x10D080) : lv_color_hex(0x666666), 0);
            lv_obj_set_style_img_recolor(id(pres5_dot), on ? lv_color_hex(0x10D080) : lv_color_hex(0x444444), 0);
            lv_obj_set_style_img_recolor_opa(id(pres5_dot), LV_OPA_COVER, 0);
  - platform: homeassistant
    id: pres6_state
    entity_id: "${pres6_entity}"
    internal: true
    on_value:
      then:
        - lambda: |-
            bool on = (x == "on");
            lv_label_set_text(id(pres6_lbl), on ? "ACTIVE" : "CLEAR");
            lv_obj_set_style_text_color(id(pres6_lbl), on ? lv_color_hex(0x10D080) : lv_color_hex(0x666666), 0);
            lv_obj_set_style_img_recolor(id(pres6_dot), on ? lv_color_hex(0x10D080) : lv_color_hex(0x444444), 0);
            lv_obj_set_style_img_recolor_opa(id(pres6_dot), LV_OPA_COVER, 0);
  - platform: homeassistant
    id: pres7_state
    entity_id: "${pres7_entity}"
    internal: true
    on_value:
      then:
        - lambda: |-
            bool on = (x == "on");
            lv_label_set_text(id(pres7_lbl), on ? "ACTIVE" : "CLEAR");
            lv_obj_set_style_text_color(id(pres7_lbl), on ? lv_color_hex(0x10D080) : lv_color_hex(0x666666), 0);
            lv_obj_set_style_img_recolor(id(pres7_dot), on ? lv_color_hex(0x10D080) : lv_color_hex(0x444444), 0);
            lv_obj_set_style_img_recolor_opa(id(pres7_dot), LV_OPA_COVER, 0);
  - platform: homeassistant
    id: pres8_state
    entity_id: "${pres8_entity}"
    internal: true
    on_value:
      then:
        - lambda: |-
            bool on = (x == "on");
            lv_label_set_text(id(pres8_lbl), on ? "ACTIVE" : "CLEAR");
            lv_obj_set_style_text_color(id(pres8_lbl), on ? lv_color_hex(0x10D080) : lv_color_hex(0x666666), 0);
            lv_obj_set_style_img_recolor(id(pres8_dot), on ? lv_color_hex(0x10D080) : lv_color_hex(0x444444), 0);
            lv_obj_set_style_img_recolor_opa(id(pres8_dot), LV_OPA_COVER, 0);
  - platform: homeassistant
    id: pres9_state
    entity_id: "${pres9_entity}"
    internal: true
    on_value:
      then:
        - lambda: |-
            bool on = (x == "on");
            lv_label_set_text(id(pres9_lbl), on ? "ACTIVE" : "CLEAR");
            lv_obj_set_style_text_color(id(pres9_lbl), on ? lv_color_hex(0x10D080) : lv_color_hex(0x666666), 0);
            lv_obj_set_style_img_recolor(id(pres9_dot), on ? lv_color_hex(0x10D080) : lv_color_hex(0x444444), 0);
            lv_obj_set_style_img_recolor_opa(id(pres9_dot), LV_OPA_COVER, 0);
  - platform: homeassistant
    id: pres10_state
    entity_id: "${pres10_entity}"
    internal: true
    on_value:
      then:
        - lambda: |-
            bool on = (x == "on");
            lv_label_set_text(id(pres10_lbl), on ? "ACTIVE" : "CLEAR");
            lv_obj_set_style_text_color(id(pres10_lbl), on ? lv_color_hex(0x10D080) : lv_color_hex(0x666666), 0);
            lv_obj_set_style_img_recolor(id(pres10_dot), on ? lv_color_hex(0x10D080) : lv_color_hex(0x444444), 0);
            lv_obj_set_style_img_recolor_opa(id(pres10_dot), LV_OPA_COVER, 0);
  - platform: homeassistant
    id: pres11_state
    entity_id: "${pres11_entity}"
    internal: true
    on_value:
      then:
        - lambda: |-
            bool on = (x == "on");
            lv_label_set_text(id(pres11_lbl), on ? "ACTIVE" : "CLEAR");
            lv_obj_set_style_text_color(id(pres11_lbl), on ? lv_color_hex(0x10D080) : lv_color_hex(0x666666), 0);
            lv_obj_set_style_img_recolor(id(pres11_dot), on ? lv_color_hex(0x10D080) : lv_color_hex(0x444444), 0);
            lv_obj_set_style_img_recolor_opa(id(pres11_dot), LV_OPA_COVER, 0);
  - platform: homeassistant
    id: pres12_state
    entity_id: "${pres12_entity}"
    internal: true
    on_value:
      then:
        - lambda: |-
            bool on = (x == "on");
            lv_label_set_text(id(pres12_lbl), on ? "ACTIVE" : "CLEAR");
            lv_obj_set_style_text_color(id(pres12_lbl), on ? lv_color_hex(0x10D080) : lv_color_hex(0x666666), 0);
            lv_obj_set_style_img_recolor(id(pres12_dot), on ? lv_color_hex(0x10D080) : lv_color_hex(0x444444), 0);
            lv_obj_set_style_img_recolor_opa(id(pres12_dot), LV_OPA_COVER, 0);

sensor:
  - platform: uptime
    name: "Panel Uptime"
  - platform: wifi_signal
    name: "Panel Wi-Fi RSSI"
    id: panel_wifi_rssi
    update_interval: 30s

binary_sensor:
  - platform: status
    name: "Panel Online"
